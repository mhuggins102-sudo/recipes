name = "recipe-tabulator"

# Per-IP daily rate limit for /api/convert (functions/_middleware.ts).
# Create the namespace and paste its id here (see README "Rate limiting"):
#   npx wrangler kv namespace create RATE_KV
# [[kv_namespaces]]
# binding = "RATE_KV"
# id = "<paste the id printed by the command above>"
compatibility_date = "2026-07-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"
